---
package: "@quen-ui/components"
title: Switch
order: 1
group: "components"
subGroup: "Form and data input elements"
description: The Switch component is a toggle element for enabling/disabling states. Features include label positioning, size variants, and accessibility compliance. Ideal for settings toggles, feature flags, and binary options.
import: "import { Switch } from '@quen-ui/components';"
source: "/packages/components/src/Switch/Switch.tsx"
excludeDemoProps: ["defaultChecked"]
---

import { Flex, Switch } from "@quen-ui/components";
import { ThumbSwitch } from "../../examples/Switch";

#### Basic usage

The `Switch` is used to toggle a single setting on or off.
It supports both controlled (via `value`) and uncontrolled (via `defaultChecked`) usage.

<Switch defaultChecked label="Email notifications" size="l" />

```tsx
function Demo() {
  const [notifications, setNotifications] = useState(true);
  return (
    <Switch
      value={notifications}
      onChange={(checked) => setNotifications(checked)}
      label="Email notifications"
      size="l"
      defaultChecked
    />
  );
}
```

#### Label positioning

You can display the label before or after the switch. By default, the label is rendered after the toggle.

<Switch label="Dark Mode" />
<br />
<Switch label="Dark Mode" labelPosition="after" />

```tsx
// Label before switch
<Switch label="Dark Mode" />

// Label after switch (default)
<Switch label="Dark Mode" labelPosition="after" />
```

#### Disabled state

Use `disabled` to make the switch non-interactive. This is useful in read-only forms or when an option is not currently available.

<Switch label="Advanced mode" disabled size="s" />

```tsx
<Switch label="Advanced mode" disabled size="s" />
```

#### Size variants

Switch supports multiple sizes (`xs`, `s`, `m`, `l`). Use larger sizes for touch devices or when accessibility is a concern.

<Flex direction="column" gap={16}>
  <Switch label="Size L" size="l" />
  <Switch label="Size M(default)" size="m" />
  <Switch label="Size S" size="s" />
  <Switch label="Size XS" size="xs" />
</Flex>

```tsx
<Switch
  label="Size L"
  size="l"
/>
<Switch
  label="Size M(default)"
  size="m"
/>
<Switch
  label="Size S"
  size="s"
/>
<Switch
  label="Size XS"
  size="xs"
/>
```

#### Thumb icon

Use `thumbIcon` to add an icon inside the thumb switch.

<ThumbSwitch />

```tsx
const ThumbSwitch = () => {
  const [checked, setChecked] = useState(false);

  return (
    <Switch
      value={checked}
      onChange={setChecked}
      thumbIcon={checked ? <IconMoon /> : <IconSun />}
    />
  );
}
```

#### Best practices

##### When to use

  - For binary on/off settings
  - For immediate actions (e.g. toggling dark mode, notifications)
  - Inside forms when a boolean value is needed

##### When not to use

  - For multi-choice selections — use radio buttons or checkboxes instead
  - For actions that require confirmation — use a button or dialog
  - Without context — always provide a label or surrounding description
